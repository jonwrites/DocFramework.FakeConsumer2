# JLib Messaging

JLib Messaging provides simple abstractions for sending messages through different delivery mechanisms.

The library separates the concept of sending a message from the mechanism used to deliver it. This allows applications to work with a common `IMessageSender` interface while choosing between different implementations.

The library is intentionally lightweight and is intended to demonstrate how generated API documentation can be combined with human-authored documentation.